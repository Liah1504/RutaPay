# Database
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=rutapay
DB_HOST=localhost
DB_PORT=5001

# JWT
JWT_SECRET=rutapay_secret_2024

# Server
PORT=5002

# --- MAIL / PASSWORD RESET (DEV: Mailtrap) ---
# Reemplaza los valores SMTP_USER y SMTP_PASS por los que te da Mailtrap
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=<TU_MAILTRAP_USER_AQUI>
SMTP_PASS=<TU_MAILTRAP_PASS_AQUI>

# Dirección FROM que verán los usuarios (puedes personalizar)
FROM_EMAIL="RutaPay <no-reply@rutapay.local>"

# URL del frontend (para construir enlaces en correos)
FRONTEND_URL=http://localhost:3000

# --- Opcional: ajustes de seguridad / alternativas ---
# Ejemplo para Gmail (usar App Password si tienes 2FA):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tuemail@gmail.com
# SMTP_PASS=<TU_APP_PASSWORD_GMAIL>

# Nota:
# - No subas este archivo al repositorio. Añade backend/.env a .gitignore.
# - Después de editar este archivo, reinicia el backend (por ejemplo: cd backend && npm run dev).